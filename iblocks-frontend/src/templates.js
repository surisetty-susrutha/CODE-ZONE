const java = `/*empty the Editor and change the language for template*/
import java.util.*;
public class hello{
    public static void main(String args[]){
          
    }
}
`
const cpp_competitive = `/*empty the Editor and change the language for template*/
#include<bits/stdc++.h>
#define ll long long
using namespace std;
void solve()
{

}
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int t;
    cin>>t;
    while(t--)
    {
        solve();
    }
    return 0;
}
`
const c = `/*empty the Editor and change the language for template*/
#include<stdio.h>
int main()
{
    /*code here */
}
`
const python = `#empty the Editor and change the language for template
print("namaste world")`;

const javascript = `/*empty the Editor and change the language for template*/
console.log("console log it ")`;

const template = [java,python,cpp_competitive,c,javascript];

export default template;